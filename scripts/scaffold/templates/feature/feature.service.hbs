import type { FastifyInstance } from "fastify";
import type { {{serviceInterface}} } from "./{{featureName}}.types";

export function {{serviceFunction}}(fastify: FastifyInstance): {{serviceInterface}} {
  return {};
}
